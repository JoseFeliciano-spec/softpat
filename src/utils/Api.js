import firebase from "./Firebase";
import "firebase/firestore";

const db = firebase.firestore(firebase);

/* export async function exitNoSerie(noSerie){
    let ok = false;
    
    return ok;
} */

